/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import uWS from "uWebSockets.js";
import http from "http";
import querystring from "querystring";
import EventEmitter from "events";
import { Socket } from "./Socket.js";
import { Application } from "./Application.js";
export declare class IncomingMessage extends EventEmitter implements http.IncomingMessage {
    #private;
    private req;
    private res;
    private app;
    url: string;
    originalUrl: string;
    method: string;
    private _baseUrl;
    private _rawquery;
    private _query;
    private _headers;
    private _params;
    private _bodydata;
    private _rawbody;
    private _remoteAddress;
    private _readableState;
    private _readBodyMaxTime;
    aborted: boolean;
    socket: Socket;
    private parameterNames;
    constructor(req: uWS.HttpRequest, res: uWS.HttpResponse, app: Application, initialData?: {
        headers?: http.IncomingHttpHeaders;
        url?: string;
        method?: string;
        body?: any;
        query?: string;
        remoteAddress?: ArrayBuffer;
    });
    get ip(): string;
    set body(_body: any);
    get body(): any;
    get headers(): http.IncomingHttpHeaders;
    set params(value: {
        [name: string]: string;
    });
    get params(): {
        [name: string]: string;
    };
    get query(): querystring.ParsedUrlQuery;
    get baseUrl(): string;
    set baseUrl(baseUrl: string);
    get path(): string;
    get(name: string): string | string[];
    header(name: string): string | string[];
    accepts(...args: any[]): string | false;
    resume(): this;
    on(event: string | symbol, listener: (...args: any[]) => void): this;
    protected _readBody(): Promise<boolean>;
}
