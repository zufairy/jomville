#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/along/Documents/GitHub/dovey/node_modules/.pnpm/@colyseus+schema@3.0.76/node_modules/@colyseus/schema/node_modules:/Users/along/Documents/GitHub/dovey/node_modules/.pnpm/@colyseus+schema@3.0.76/node_modules:/Users/along/Documents/GitHub/dovey/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/Users/along/Documents/GitHub/dovey/node_modules/.pnpm/@colyseus+schema@3.0.76/node_modules/@colyseus/schema/node_modules:/Users/along/Documents/GitHub/dovey/node_modules/.pnpm/@colyseus+schema@3.0.76/node_modules:/Users/along/Documents/GitHub/dovey/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../../../@colyseus/schema/bin/schema-codegen" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../@colyseus/schema/bin/schema-codegen" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../../../@colyseus/schema/bin/schema-codegen" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../../../@colyseus/schema/bin/schema-codegen" "$@"
else
  exec node  "$basedir/../../../@colyseus/schema/bin/schema-codegen" "$@"
fi
# cmd-shim-target=/Users/along/Documents/GitHub/dovey/node_modules/.pnpm/colyseus@0.16.5_@colyseus+auth@0.16.6_@colyseus+core@0.16.26_@colyseus+schema@3.0.76_@p_173f2ced8301f324d78aaf25b29f4737/node_modules/@colyseus/schema/bin/schema-codegen
